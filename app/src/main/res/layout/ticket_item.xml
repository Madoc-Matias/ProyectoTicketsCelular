<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Título del Ticket -->
    <TextView
        android:id="@+id/ticketTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Título del Ticket"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/ticketStatus"
        android:layout_marginBottom="8dp" />

    <!-- Estado del Ticket -->
    <TextView
        android:id="@+id/ticketStatus"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Estado del Ticket"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/ticketTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/ticketFallos"
        android:layout_marginBottom="8dp" />

    <!-- Fallos del Ticket -->
    <TextView
        android:id="@+id/ticketFallos"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Fallos"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@id/ticketStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:visibility="gone" />

</androidx.constraintlayout.widget.ConstraintLayout>

